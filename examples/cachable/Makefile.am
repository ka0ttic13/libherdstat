# $Id$

include $(top_builddir)/examples/Makefile.am.common

CLEANFILES = *.txt
MAINTAINERCLEANFILES = Makefile.in *~

if BUILD_EXAMPLES
noinst_PROGRAMS = metadata_list
metadata_list_SOURCES = main.cc
endif
